.ai/
__pycache__/
*.sqlite3
.env
*/.env
.env.uat
staticfiles/
resources
# Generated FS storage for uploads + per-job deliverables (USE_S3=False dev
# path; ScoringStorage's default root). Prod uses S3, so this is dev-only.
scoring_storage/
# Blackbox system-test run artifacts (SQLite DBs, captured server logs, API
# transcripts) — created fresh per run under results/<run-id>/. Never commit.
results/
.zcode/

# Docker dev artifacts (named-volume data lives in Docker, not the repo tree).
# The compose files mount these as named volumes; this guard is for any stray
# bind-mount exports or local dumps.
minio-data/
db-data/