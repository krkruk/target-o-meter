// Dashboard route — S-02 Phase 7 fills this in (single-screen CSS Grid: hero
// stats, add-photos button, results list, daily-average recharts chart).
// Phase 6 ships a stub so the router table resolves.
export function Dashboard() {
  return (
    <div data-testid="dashboard-route">
      {/* TODO Phase 7: single-screen dashboard */}
    </div>
  );
}
