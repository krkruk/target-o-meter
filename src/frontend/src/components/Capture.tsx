// Capture route — S-02 Phase 8 fills this in (mobile camera capture wizard).
// Phase 6 ships a stub so the router table resolves.
export function Capture() {
  return (
    <div data-testid="capture-route">
      {/* TODO Phase 8: mobile camera capture */}
    </div>
  );
}
