// Results route — S-02 Phase 8 fills this in (marked image + per-hole
// correction dropdowns). Phase 6 ships a stub so the router table resolves.
export function Results() {
  return (
    <div data-testid="results-route">
      {/* TODO Phase 8: marked image + per-hole corrections */}
    </div>
  );
}
