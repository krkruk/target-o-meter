"""Eval subpackage — diagnostic/test-only tooling (score Jaccard, metadata loader).

NOT imported by ``services`` or ``models`` (production path keeps no eval
dependency; AGENTS.md §1 — DB stores metadata only).
"""
