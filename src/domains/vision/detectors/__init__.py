"""Detectors subpackage — ``HoleDetector`` strategies + locked schema/prompt/client.

Each concrete detector lives in its own module per the one-class-per-file rule
(``context/foundation/lessons.md`` § "One class per file, matching filename").
"""
