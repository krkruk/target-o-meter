Throwaway smoke trigger for the ast-grep rename (reviewer-target-o-meter
context/changes/ast-grep-fix Phase 5). Exists only to fire the review workflow
against the unmerged @feature/update-ast-grep-configuration branch. Close this
PR without merging; delete the branch after the run.
